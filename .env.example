# Production env vars (voorbeeld — GEEN echte secrets committen)
RAILS_ENV=production
SECRET_KEY_BASE=change-me-in-prod-env-file
RAILS_LOG_LEVEL=info
# Voeg hier evt. SENDGRID_API_KEY / REDIS_CACHE_URL / DATABASE_URL, etc.
