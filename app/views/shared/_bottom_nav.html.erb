<%# Fixed bottom navigation (mobile-first) %>
<nav class="sf-bottom-nav" role="navigation" aria-label="Main">
  <%= link_to root_path, class: "sf-tab #{'is-active' if current_page?(root_path)}" do %>
    <span class="sf-ico">🏠</span><span>Home</span>
  <% end %>

  <%= link_to classes_path, class: "sf-tab #{'is-active' if request.path.starts_with?('/classes')}" do %>
    <span class="sf-ico">📅</span><span>Lessen</span>
  <% end %>

  <%= link_to account_bookings_path, class: "sf-tab #{'is-active' if request.path.starts_with?('/account/bookings')}" do %>
    <span class="sf-ico">🗓️</span><span>Boekingen</span>
  <% end %>

  <%= link_to account_credits_path, class: "sf-tab #{'is-active' if request.path.starts_with?('/account/credits')}" do %>
    <span class="sf-ico">🪙</span><span>Credits</span>
  <% end %>

  <%= link_to spree.account_path, class: "sf-tab #{'is-active' if request.path.starts_with?('/account') && !request.path.starts_with?('/account/credits') && !request.path.starts_with?('/account/bookings')}" do %>
    <span class="sf-ico">👤</span><span>Account</span>
  <% end %>
</nav>
