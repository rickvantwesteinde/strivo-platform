<%# Fixed bottom navigation (mobile-first) %>
<nav class="sf-bottom-nav" role="navigation" aria-label="Main">
  <%= link_to root_path, class: "sf-tab #{'is-active' if current_page?(root_path)}" do %>
    <span class="sf-ico">🏠</span><span>Home</span>
  <% end %>

  <%= link_to storefront_account_bookings_path, class: "sf-tab #{'is-active' if request.path.starts_with?('/account/bookings')}" do %>
    <span class="sf-ico">🗓️</span><span>Bookings</span>
  <% end %>

  <%= link_to club_path, class: "sf-tab #{'is-active' if request.path.starts_with?('/club')}" do %>
    <span class="sf-ico">🏛️</span><span>Club</span>
  <% end %>

  <%= link_to spree.account_path, class: "sf-tab #{'is-active' if request.path.starts_with?('/account') && !request.path.starts_with?('/account/bookings')}" do %>
    <span class="sf-ico">👤</span><span>Profile</span>
  <% end %>
</nav>